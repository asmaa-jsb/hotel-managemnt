import { Button } from "@mui/material";

const HomePage = () => {
  return (
    <>
      <div>HomePage</div>
      <Button type="submit" className="submit-btn" fullWidth variant="outlined">
        Log out
      </Button>
    </>
  );
};

export default HomePage;
