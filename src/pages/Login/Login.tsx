import { loginBackground } from "@/assets/Images";

const Login = () => {
  return (
    <div>
      <img src={loginBackground} alt="login" />
      <h1>Login</h1>
    </div>
  );
};

export default Login;
